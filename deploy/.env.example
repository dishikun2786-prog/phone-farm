# Control Server
JWT_SECRET=change-me-in-production-use-random-string
DEVICE_AUTH_TOKEN=device-auth-token-change-me

# PostgreSQL
POSTGRES_USER=phonefarm
POSTGRES_PASSWORD=phonefarm_secret
POSTGRES_DB=phonefarm

# Headscale (already configured)
HEADSCALE_API_URL=http://headscale:8080

# Redis
REDIS_URL=redis://redis:6379
